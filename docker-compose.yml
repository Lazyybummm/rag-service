version: '3.8'

services:
  rag-service:
    build: .
    container_name: aicos-rag-service
    restart: always
    environment:
      # Database Connection - Hardcoded
      - DATABASE_URL=postgresql://neondb_owner:npg_V2Bnuz5xFpYe@ep-nameless-bird-a1pl9bnj-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      
      # DeepSeek API Key - Hardcoded
      - DEEPSEEK_API_KEY=sk-8410023103ce4edd9e12993623ba1dc6
      
      # Port - Hardcoded
      - PORT=8001
      
    ports:
      - "8083:8001"
    volumes:
      - ./logs:/app/logs
    command: uvicorn main:app --host 0.0.0.0 --port 8001
